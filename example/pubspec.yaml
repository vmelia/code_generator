name: example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  generators:
    path: ../generators/

  cupertino_icons:

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:


  flutter_lints:

flutter:
  uses-material-design: true
